graph TB
    subgraph External["External Clients"]
        GK["Guest Kiosk / Mobile"]
        SD["Staff Dashboard"]
        MD["Manager Dashboard"]
        AD["Admin Panel"]
    end

    subgraph Cloud["Cloud Services"]
        GROQ["Groq Cloud API<br/>llama-3.1-8b-instant<br/>Primary AI"]
    end

    subgraph Vercel["Vercel"]
        FE["frontend<br/>Next.js + shadcn/ui"]
    end

    subgraph Railway["Railway"]
        API["api :4000<br/>Elysia + Bun<br/>REST + WebSocket + SSE"]
        WK["worker ×2<br/>Bun + BullMQ"]
        OL["ollama :11434<br/>llama3 (fallback)"]
        WH["whisper :8000<br/>faster-whisper-server"]
        PG["postgres :5432<br/>PostgreSQL 16 + RLS"]
        RD["redis :6379<br/>Queue + Pub/Sub + Cache"]
    end

    GK -->|"HTTPS"| FE
    SD -->|"HTTPS + WSS"| FE
    MD -->|"HTTPS + WSS"| FE
    AD -->|"HTTPS"| FE

    FE -->|"/api/*"| API

    API -->|"enqueue"| RD
    API -->|"read/write"| PG
    API <-->|"pub/sub"| RD

    WK -->|"dequeue"| RD
    WK -->|"HTTPS"| GROQ
    WK -.->|"fallback"| OL
    WK -->|"HTTP"| WH
    WK -->|"read/write"| PG
    WK -->|"publish"| RD
